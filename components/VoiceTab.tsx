import React from 'react';

// Voice features have been removed from the application.
// This component is kept as a placeholder to prevent build errors from unused imports.

export const VoiceTab: React.FC<any> = () => {
  return (
    <div className="flex items-center justify-center h-full text-gray-500">
      Voice features disabled
    </div>
  );
};
